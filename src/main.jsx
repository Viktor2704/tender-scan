import React from 'react'
import ReactDOM from 'react-dom/client'
import TenderApp from './App.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <TenderApp />
  </React.StrictMode>
)
